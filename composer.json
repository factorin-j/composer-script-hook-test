{
    "require-dev": {
        "phpunit/phpunit": "4.7.7"
    },
    "scripts": {
        "post-install-cmd": ["@phpunit"],
        "post-update-cmd": ["@phpunit"],
        "phpunit": ["phpunit --colors=always --verbose"]
    }
}
